{
  "name": "vite",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "concurrently \"cd packages/server && npm start\" \"cd packages/client && npm run dev\"",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.5"
  },
  "dependencies": {
    "concurrently": "^7.1.0"
  }
}
